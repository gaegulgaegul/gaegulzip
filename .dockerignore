# Monorepo .dockerignore for apps/server deployment
# Docker Context: . (root)

# Mobile app (not needed for server deployment)
apps/mobile/

# Dependencies (will be installed in Docker)
**/node_modules/
**/.pnpm-store/

# Build artifacts (will be built in Docker)
**/dist/
**/*.tsbuildinfo

# Environment files (use Render dashboard for env vars)
**/.env
**/.env.*

# Tests
**/tests/
**/*.test.ts
**/*.spec.ts
**/coverage/
**/.nyc_output/

# Development tools
**/.vscode/
**/.idea/
**/.DS_Store
**/*.swp
**/*.swo
**/*~

# Git
.git/
.gitignore

# Logs
**/logs/
**/*.log

# CI/CD
.github/
.gitlab-ci.yml
.circleci/

# Other
**/tmp/
**/temp/
**/.cache/
**/.turbo/
prompt/
.claude/

# Specific root files to exclude
turbo.json
melos.yaml
pubspec.yaml
gaegulzip.iml
bkit.config.json

# Keep these important files (explicitly listed to avoid wildcards)
# - pnpm-lock.yaml (needed for monorepo)
# - pnpm-workspace.yaml (needed for pnpm workspace)
# - apps/server/** (all server files)
