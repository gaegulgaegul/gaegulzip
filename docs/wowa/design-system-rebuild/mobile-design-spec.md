# Mobile Design Spec: SketchInput 리빌드

## 1. 컴포넌트 구조

### SketchInputMode (enum)

```
SketchInputMode
├── defaultMode  — 기본 텍스트 입력
├── search       — 검색 입력 (돋보기 아이콘 + 지우기 버튼)
├── date         — 날짜 입력 (YYYY/MM/DD)
├── time         — 시간 입력 (HH:MM AM/PM)
└── datetime     — 날짜+시간 입력
```

## 2. 레이아웃 설계

### Default Mode
```
┌─ Label (옵셔널, 항상 상단 고정) ─────────────┐
│  "Label"  (hand font, 14sp)                  │
├──────────────────────────────────────────────┤
│ ╭~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╮    │
│ │  [prefixIcon?]  입력 텍스트  [suffixIcon?] │ │  ← SketchPainter 테두리 (불규칙)
│ ╰~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╯    │     높이: 44px
├──────────────────────────────────────────────┤
│  helperText / errorText (옵셔널)              │
└──────────────────────────────────────────────┘
```

### Search Mode
```
╭~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╮
│  🔍(sketch)  Search       [✕ clear]   │  ← 돋보기 아이콘은 스케치 스타일
╰~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╯     지우기 버튼은 텍스트 입력 시만 표시
```

- 돋보기 아이콘: `Icons.search` (기존 Material icon 사용)
- hint: "Search" (기본값, 커스터마이징 가능)
- 지우기 버튼: 텍스트가 있을 때만 표시

### Date Mode
```
╭~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╮
│  2024/09/02                           │  ← readOnly, 탭 시 DatePicker
╰~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╯
```

- 텍스트 포맷: `YYYY/MM/DD`
- readOnly 입력 (직접 타이핑 불가)
- 탭 시 onTap 콜백 호출 (DatePicker는 외부에서 처리)

### Time Mode
```
╭~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╮
│  09:24 AM                             │  ← readOnly, 탭 시 TimePicker
╰~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╯
```

- 텍스트 포맷: `HH:MM AM/PM`
- readOnly 입력
- 탭 시 onTap 콜백 호출

### DateTime Mode
```
╭~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╮
│  2024/09/02  09:24 AM                 │  ← readOnly
╰~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~╯
```

## 3. 상태별 디자인

### Light Mode

| 상태 | 테두리 색상 | 채우기 | 스트로크 |
|------|-----------|--------|---------|
| Normal | base900 (#343434) | white (#FFFFFF) | strokeBold (3.0) |
| Focused | black (#000000) | white | strokeThick (4.0) |
| Error | error (red) | white | strokeBold (3.0) |
| Disabled | base300 (#DCDCDC) | base100 (#F7F7F7) | strokeStandard (2.0) |

### Dark Mode

| 상태 | 테두리 색상 | 채우기 | 스트로크 |
|------|-----------|--------|---------|
| Normal | white (#FFFFFF) | backgroundDark (#1A1D29) | strokeBold (3.0) |
| Focused | white (#FFFFFF) | backgroundDark | strokeThick (4.0) |
| Error | error (red) | backgroundDark | strokeBold (3.0) |
| Disabled | base700 (#5E5E5E) | surfaceVariantDark (#2C3048) | strokeStandard (2.0) |

### 핵심 변경: 스트로크 두께

디자인 이미지에서 테두리가 기존 `strokeStandard (2.0)`보다 두꺼움.
→ 기본 스트로크를 `strokeBold (3.0)`으로 변경하여 이미지 질감 재현.

## 4. SketchPainter 설정

```
roughness: 0.8 (SketchDesignTokens.roughness — 기본값)
bowing: 0.5 (SketchDesignTokens.bowing — 기본값)
enableNoise: true (종이 질감 표현)
borderRadius: 12.0 (irregularBorderRadius)
seed: hashCode 기반 (재현 가능한 불규칙 테두리)
```

## 5. 크기 사양

| 항목 | 값 |
|------|-----|
| 입력 필드 높이 | 44px (기존과 동일) |
| 라벨-필드 간격 | 6px |
| 필드-도움말 간격 | 4px |
| 내부 수평 패딩 | spacingMd (12px) |
| 내부 수직 패딩 | spacingSm (8px) |
| 아이콘 크기 | 20px |
| 아이콘-텍스트 간격 | spacingSm (8px) |

## 6. 폰트 사양

| 요소 | 폰트 | 크기 | 무게 |
|------|------|------|------|
| Label | hand font | fontSizeSm (14) | w500 |
| Input text | hand font | fontSizeBase (16) | normal |
| Hint text | hand font | fontSizeBase (16) | normal |
| Helper/Error | hand font | fontSizeXs (12) | normal |

## 7. 인터랙션

### Focus 효과
- 스트로크 두께 증가: strokeBold → strokeThick
- 테두리 색상 변경 (light: black, dark: white)

### Search Mode 지우기 버튼
- 텍스트가 비어있으면 숨김
- 텍스트가 있으면 표시 (애니메이션 없이 즉시)
- 탭 시 텍스트 클리어 + onChanged('') 호출

### Date/Time/DateTime Mode
- readOnly = true (키보드 미표시)
- onTap 콜백으로 외부 picker 트리거
- 값이 없으면 hint 텍스트 표시
